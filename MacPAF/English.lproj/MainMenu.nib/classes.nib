{
    IBClasses = (
        {
            CLASS = ApplicationUtilities; 
            LANGUAGE = Java; 
            OUTLETS = {progress = NSProgressIndicator; splashScreen = NSPanel; }; 
            SUPERCLASS = "java.lang.Object"; 
        }, 
        {
            ACTIONS = {
                addNewChild = id; 
                addNewFamily = id; 
                addNewFamilyAsChild = id; 
                addNewFamilyAsSpouse = id; 
                addNewFather = id; 
                addNewIndividual = id; 
                addNewMother = id; 
                addNewSpouse = id; 
                exportFile = id; 
                forgetIndividual = id; 
                importFile = id; 
                openReportsSheet = id; 
                recallIndividual = id; 
                recallLastFoundIndividual = id; 
                recallLastSavedIndividual = id; 
                rememberIndividual = id; 
                showFamilyList = id; 
                showIndividualList = id; 
            }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}